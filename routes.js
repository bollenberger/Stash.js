exports.create = function() {
    var routes = {}
    
    return {
        insert: function(id, neighbor, cost) {
            
            var routes_to_id = routes[id];
            var i = 0;
            while (i < routes_to_id.length) {
            }
            
        }
    };
};
